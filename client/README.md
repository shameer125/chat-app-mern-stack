# QuickChat — frontend

React + Vite SPA for **QuickChat**. API and Socket.IO are proxied to the backend in dev (`vite.config.js`).

**Setup, env vars, two-device testing, and calls troubleshooting:** see the [**main README**](../README.md) in the repo root.

```bash
npm install
npm run dev
```
